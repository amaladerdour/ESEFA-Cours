#include<stdio.h>
main()
{
    int a,b,x;
    printf("donner la valeur de a et b ");
    scanf("%d%d",&a,&b);
    if (a!=b )
        x=-b/a;
    else
    {if ( b=0);
    prinft("il y a pluiseur solutions");}
    else
   {

   } printf("il y a aucan solution ");
    }

}
